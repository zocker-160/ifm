#! /bin/sh

$BUILDDIR/src/ifm -I$SRCDIR/lib -w -task -format raw 2>&1 > $BUILDDIR/tests/task-nopath.out <<END
# Test of 'nopath' attribute.

room "R1" tag R1;

room "R2" dir e;

room "R3" dir s;

room "R4" dir w tag R4;
task "Win";

link R4 to R1 nopath;
END

cmp -s $SRCDIR/tests/task-nopath.exp $BUILDDIR/tests/task-nopath.out
